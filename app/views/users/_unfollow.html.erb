<%= form_with(model: current_user.active_user_follows.find_by(followed_id: @user.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit t('.unfollow'), class: "btn btn-default" %>
<% end %>
